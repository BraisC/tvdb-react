import styled from 'styled-components';

const Overlay = styled.div`
  width: 100%;
  height: 100%;
  background-color: var(--color-transparency);
  position: fixed;
  z-index: 4;
`;

export const Styled = {
  Overlay,
};
