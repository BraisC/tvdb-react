import styled from 'styled-components';

const Overlay = styled.div`
  width: 100%;
  height: 100%;
  background-color: var(--color-transparency);
  position: absolute;
`;

export const Styled = {
  Overlay,
};
