import styled from 'styled-components';

const Casting = styled.div`
  width: 50%;
  height: 30.7rem;
  align-self: center;
  background-color: var(--color-primary);
`;

export const Styled = {
  Casting,
};
