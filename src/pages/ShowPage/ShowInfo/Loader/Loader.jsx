import React from 'react';
import { Styled } from './styled';

const Loader = (props) => (
  <Styled.ShowInfo>
    <Styled.Poster />
    <Styled.Data />
  </Styled.ShowInfo>
);

export default Loader;
