import React from 'react';
import { Styled } from './styled';

const Loader = () => (
  <Styled.PeopleInfo>
    <Styled.Profile />
    <Styled.Data />
  </Styled.PeopleInfo>
);

export default Loader;
