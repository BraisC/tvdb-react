import React from 'react';
import { Styled } from './styled';

const Loader = (props) => (
  <>
    <Styled.Header />
    <Styled.Wrapper>
      <Styled.Items />
    </Styled.Wrapper>
  </>
);

export default Loader;
